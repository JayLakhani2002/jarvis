# secrets & machine-local state — never commit
config/*
!config/*.example
finance/
logs/
dashboard/

# personal scratch (kept out of the public repo)
prompt.md

# build & OS noise
__pycache__/
.DS_Store
app/node_modules/
*.zip

# eval goldens contain real vault questions — the example is the public template
evals/goldens.json
evals/results/
