{
  "name": "jarvis-app",
  "version": "0.1.0",
  "description": "Jarvis v2 command center — Electron shell over the existing Jarvis backend",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node guard.js && node jobs.js && node voice.js"
  },
  "license": "UNLICENSED",
  "private": true,
  "type": "commonjs",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.211"
  },
  "devDependencies": {
    "electron": "^43.1.1"
  }
}
